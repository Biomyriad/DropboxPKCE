SETUP TERMUX
  only dounload APK!
  https://f-droid.org/en/packages/com.termux/
  ---------------
  
  termux-setup-storage
  
  pkg upgrade (select yes to all questions)
  
  pkg install build-essential (recommended)
  

INSTALL PYTHON
  extra packages:
  https://wiki.termux.com/wiki/Python
  ---------------
  
    pkg install rust
    
    pkg install python
    
    pip install pipenv


INSTALL MYSQL (MARIADB)
  link for info: 
  https://www.termuxcommands.com/how-to-install-mysql-in-termux/
  ------------------------
  
    pkg install mariadb -y
  
  // start background server 
  // (in another term or in background process)
  for terminal:
    mysqld
    mariadb
  
  for background process (job):
    mysqld & 
    mariadb &
  
  // DB config:
    mysql_secure_installation
    OR
    mariadb-secure-installation
  //
  
      =================>
      *==== questions walk through ====*
      ---------‐-------------------------
      
      current password for root? (its blank just hit enter)
      
      unix_socket? (select NO!)
      
      set root password? (Yes maybe to "root")
      
      remove anon users? (yes)
      
      disallow root login remotely? (NO!)
      
      remove test DB's? (yes)
      
      reload privilege tables? (yes)
      
      DONE.
      ================== !
      
  To run mysql shell use:
    mysql -u root -p
    mariadb -u root -p

